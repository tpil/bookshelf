import { NoImageContainer } from './no-image.styles';

const NoImage = () => {

  return (
    <NoImageContainer>
      <span>No image available</span>
    </NoImageContainer>
  );
};
export default NoImage;
